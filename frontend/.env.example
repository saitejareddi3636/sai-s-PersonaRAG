# Copy to .env.local for local development.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Vercel / HTTPS: browser calls same-origin /api/*; the proxy needs the real API URL.
# Set both to your OCI backend, e.g. http://YOUR_PUBLIC_IP:8000
# BACKEND_ORIG=http://YOUR_PUBLIC_IP:8000
