# Copy to .env and fill in locally. Do not commit secrets.

# Frontend (Next.js)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Backend (FastAPI)
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:3000

# Future: embeddings / vector DB / LLM keys (add when wired)
# OPENAI_API_KEY=
# VECTOR_DB_URL=
