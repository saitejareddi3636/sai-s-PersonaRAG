# Copy to repo-root `.env` for `docker compose` variable substitution.
# Example:  cp compose.env.example .env
#
# Vercel frontend origin (exact URL). Comma-separate multiple origins.
# CORS_ORIGINS=https://your-app.vercel.app
